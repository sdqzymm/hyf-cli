'use strict';

function init(projectName, opts, cmd) {
  // console.log('init', projectName, opts.force, cmd.parent.opts().targetPath, process.env.CLI_TARGET_PATH)
  console.log('init命令')
}

module.exports = init;
